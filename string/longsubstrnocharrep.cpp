#include<bits/stdc++.h>
using namespace std;
int lsub(string s)
{
    int start=-1;
    int maxln=0;
    int arr[26];
    int arrSize = sizeof(arr)/sizeof(arr[0]);
    for (int i = 0; i < arrSize; i++)
    {
        int pos=arr[i]-'a';
        
        /* code */
    }
    


}